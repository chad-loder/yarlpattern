# Reference

Auto-extracted reference material.

- [**API**](api.md) — every public name re-exported from
  `yarlpattern/__init__.py`, rendered by `mkdocstrings` from the
  source's Google-style docstrings. Tiered by audience: primary API
  (`URLPattern`, `URLPatternResult`, `COMPONENTS`), escape helpers,
  and the lower-level tokenizer / parts / regex-generation building
  blocks for tools that compose URLPatterns programmatically.

For background on *how* the pieces named here fit together, see
[Architecture](../explanation/architecture.md).
